[build]
  command   = "chmod +x ./build.sh && ./build.sh"
  publish   = "build/web"

[build.environment]
  FLUTTER_CHANNEL = "stable"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
